# TH-P05-08 — Clean Environment Deployment Verification（执行记录）

- **任务**: 空环境部署验证（B8）
- **依赖**: TH-P05-07（DONE）
- **执行批次**: Sprint 1 Batch 3
- **日期**: 2026-09-03
- **状态**: DONE

## 1. 环境记录（image / commit / migration version）

| 项 | 值 |
|---|---|
| 构建来源 commit | `279761a`（TH-P05-04 完成后的 main；播种/对账一次性脚本为本次新增） |
| 构建命令 | `go build -trimpath -ldflags "-s -w" -o bin/{api,worker}`（与手册 §2 一致） |
| Go | go1.26.4 darwin/arm64（模块 go1.22） |
| PostgreSQL | 17.10 (Homebrew)，localhost:5432 |
| Redis | 8.8.0 (Homebrew)，全新空数据集，127.0.0.1:16380，独立数据目录 |
| 一次性数据库 | `deeptrols_p0508_clean`（DROP→CREATE→迁移） |
| 迁移版本 | `000036_auth_sessions`（36 个 .up.sql 全部应用；无 schema_migrations 版本表，见手册 §5.3 口径） |
| API | 127.0.0.1:18080，生产形态配置：`ENABLE_FAKE_PAYMENT=false`、`COOKIE_SECURE=true`、强随机 `JWT_SECRET`(64B hex)/`ENCRYPTION_KEY`(32B)/`ADMIN_PASSWORD`(≥12B) |
| 假 Provider | 仓库自带 `scripts/echo_upstream`（E2E 工具）127.0.0.1:8090，无外网 |

**记录在案的偏差**（一次性环境与生产的差异）：

1. 未使用容器镜像：本机无法访问 Docker Hub（`docker pull redis:7-alpine` 网络超时），
   改用宿主 PostgreSQL 17.10 与 Homebrew Redis 8.8.0（compose 声明
   `postgres:16-alpine` / `redis:7-alpine`）。行为等价；版本差异在此记录。
2. `golang-migrate` CLI 未安装（手册 §3 旧文引用它）：按裸 SQL 文件名顺序
   + `psql -v ON_ERROR_STOP=1 -1` 逐文件单事务应用——这与仓库真实机制
   （§5.3 已声明不维护版本表）一致；手册 §3 已同步修正。
3. 对账器生产环境为 1 小时 ticker：本次用一次性驱动
   `scripts/p0508_recon_driver.go` 直接调用同一 `reconciliation.Run`
   生产代码路径；worker 侧 lease 门控另行经 Redis `worker:lease:*` 键证实。
4. 运行产物（日志/二进制/环境文件）保留在 `/tmp/p0508/`（临时）；
   一次性数据库保留供复核。

## 2. Clean Deployment Matrix

| # | 步骤 | 预期 | 观察 | 证据 | 结果 |
|---|---|---|---|---|---|
| 1 | 空库创建 | 0 张表 | 0 张表 | `information_schema.tables` count=0 | PASS |
| 2 | Migration up（36 文件，逐文件单事务，ON_ERROR_STOP） | 全部成功 | `APPLIED=36`，39 张表，含扩展 | 迁移输出 + 表计数 | PASS |
| 3 | 一次安全 down（000036.down）再 up | `auth_sessions` 消失后恢复 | down 后表计数 0（该表）→ re-up 后 39 | psql 往返 | PASS |
| 4 | Fail-fast：缺 `DATABASE_URL` | 拒绝启动 | `required environment variable DATABASE_URL is not set`，进程退出 | api.log 输出 | PASS |
| 5 | Fail-fast：弱默认 `JWT_SECRET`/`ENCRYPTION_KEY`/`ADMIN_PASSWORD`（生产模式） | 拒绝启动 | `weak default secret "…" detected in production mode`（三种分别验证） | 启动输出 | PASS |
| 6 | Fail-fast：`COOKIE_SECURE=false` / 短管理员密码 / `ENCRYPTION_KEY`≠32B（生产模式） | 拒绝启动 | 分别报 `COOKIE_SECURE must be true` / `ADMIN_PASSWORD ≥12` / `exactly 32 bytes` | 启动输出 | PASS |
| 7 | API 启动 → 探针（**AC-01**） | /health、/healthz 200；/readyz 200 且 DB+Redis ok | `health=200`、`healthz=200`、`readyz=200 {"checks":{"database":"ok","redis":"ok"},"status":"ready"}`；`/metrics` 200 `text/plain` | curl + api.log | PASS |
| 8 | 启动期 admin 写入（审计标签） | 写入被打标 | admin user（nil-UUID）+ 钱包（生产模式零奖励，balance 0）；`display_name='Administrator [deployment-verification TH-P05-08 2026-09-03]'` | users 行 | PASS |
| 9 | Worker + Redis lease（**AC-02**） | 至少一个带 lease 的周期 | 启动即见 `worker:lease:subscription_expirer/renewer`（TTL≈300s）；周期日志 `health_checker: checked 1 channels`（播种后）、`billing_sync: ran 0 due connectors`，lease 键持续轮换 | worker.log + `redis-cli --scan` | PASS |
| 10 | 播种（全部打 `p0508-deployment-verification` 标签） | 用户/密钥/钱包(入账 5)/模型/定价/渠道/实例 | 8 行全部 OK；钱包经生产 `ProvisionUserWallet`+ledgered TopUp | 种子脚本输出 | PASS |
| 11 | 网关冒烟（**AC-03**） | 请求→定价→预留→executor→结算，证据齐全，扣费精确 | HTTP 200；usage_source=`upstream`；balance 5.000000→4.999997（**恰 −0.000003** = 1×1.0/1M + 1×2.0/1M）；`usage_logs`(completed) + `charge_lines`(2 行) + `provider_evidence`(echo-1, 200) + 账本 `topup,charge` 恰 2 行；frozen=0 | curl + 4 张表查询 | PASS |
| 12 | 持久化 + 重启无重复资金效果 | 重启后重放同一 X-Request-ID 不双扣 | SIGTERM→重启→readyz 200；重放 200：balance/账本行数不变（4.999997 / 2），重放仅记 cost=0 证据行；users 仍 2（ensureAdminUser 幂等） | 重启前后对账查询 | PASS |
| 13 | 对账（**AC-04**） | 产出 reconciliation_runs 行 | 1 行：level=L1、status=completed、total_requests=2、diff_count=2、completed_at 非空 | reconciliation_runs | PASS |
| 14 | 支付宝/微信配置缺失 | fail-fast 或 disabled | 代码库无任何 ALIPAY/WECHAT 配置项（官方支付未实现，P1 TODO）；启动无此依赖；`ENABLE_FAKE_PAYMENT=false` 时演示充值固定 403（`在线充值未开通`）→ **DISABLED（按设计）** | config 全量 grep + `wallet.go` 403 分支 | PASS |
| 15 | 活体资金不变量抽查（W2） | balance == 账本净额 | 两个钱包均 `t`（topup +5 / charge −0.000003×2） | SQL 断言 | PASS |
| 16 | TH-P05-04 指标活体 | 真实请求推进资金计数器 | 当前进程：新请求后 `billing_reserve_total=1`、`billing_settle_total=1`、`gateway_requests_total{chat/completions}=2`；重启后的纯重放不推进资金计数器（符合语义） | /metrics 抓取 | PASS |

## 3. 对账差异解释（diff_count=2，均 warning，仅人工复核）

- `missing_charge_lines`：重启后的**幂等重放**证据行（cost=0，无账）——
  重放本就不产生收费行，属预期形状。
- `usage_without_billing`（L3）：usage 有 provider 证据但无外部
  `billing_records`——本环境未配置任何外部计费连接器（OneAPI/NewAPI/Aliyun），
  属预期。按手册 §8，差异进入人工复核，系统不自动补扣。

## 4. 记录在案（不改代码）

- 对账 L0 把幂等重放的证据行识别为 `missing_charge_lines` warning；
  若未来重放量大，可考虑在重放证据行上打标记以便对账忽略（P5 级优化，非缺陷）。
- DEPLOYMENT.md §3 原引用 `golang-migrate` CLI 与仓库真实机制（裸 SQL、
  无版本表，§5.3 已自认）不一致，本次已同步修正并写入空环境验证结论（§9）。

## 5. Test Gate（全局）

- `go test ./... -count=1`（含 TEST_DATABASE_URL）：全绿（TH-P05-04 收口时运行）
- `go vet ./...` / `go build ./...` / `gofmt -l`：clean
- 新增两个 `//go:build ignore` 一次性脚本不进生产二进制，`go build ./...` 不受影响

## 6. Result: **DONE**
