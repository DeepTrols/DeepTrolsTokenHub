# Execution Log · TH-P05-03

## 1. Task 元信息

- Task ID: TH-P05-03
- Title: Billing Invariant And Concurrency Tests
- Phase: P0.5（Production Safety Gate）
- Dependencies: TH-P05-02（执行前校验：DONE）
- Sprint 1 Batch 3（与 TH-P05-04 / TH-P05-08 同批）

## 2. Implementation 摘要

目标：证明**当前**钱包/预留/结算/释放/提交/网关计费在重复、并发、
异常下满足资金不变量。**零行资金语义改动**——本任务只新增两个
真实 Postgres 测试文件，所有断言对照既有语义（TH-P05-02 已钉住
的 settle 兜底行为 + `postgres.go` 锁序）：

- **新增 `internal/repository/wallet/invariants_p0503_test.go`**
  钱包仓库级证明（10 个测试，全部真实 Postgres，`TEST_DATABASE_URL`
  缺失时按仓库规范 skip 并告警）。辅助函数：
  `seedP0503Wallet`（带账本的 TopUp 注资）、`p0503LedgerNet`
  （按 `reserve/release→0, charge→-amount, else +amount` 重算余额）、
  `p0503OpenReserveSum`（未完结 reserve 行之和）、
  `p0503AssertInvariants`（W1/W2/W3 非负 + W11 账本可解释）。
- **新增 `internal/handler/gateway/invariants_p0503_test.go`**
  网关级证明（6 个测试）：真实钱包仓库 + 真实 `billing.Charger`
  贯穿 `HandleNonStreamingChat`，仅 executor/usage/model/channel/
  pricing 为 mock。关键设施：
  - `p0503ObservedWallets`：包装真实仓库，统计 Reserve 尝试次数，
    可用 channel 门控第二次预留——**确定性编排并发交错**，不依赖
    调度运气，不改任何资金语义。
  - 定价 fixture 为整数/1M-token 单价（input 1.0 / output 2.0），
    标准请求（usage 20/15）精确到 6 位小数 **0.000050**；
    巨量 usage（200000 completion）精确 **0.400020**；hold 通过
    `computeMaxChargeHold` 动态发现，资金按 hold 的倍数注入，
    避免硬编码金额漂移。
  - W10 少收证据：同时断言 `usage_logs.error_code='undercharged'`、
    `WalletCharged==hold`、进程内 `UnderchargeFallbackCounts()`
    `chat|gpt-4o` +1（TH-P05-04 会把该计数接入 Prometheus）。

## 3. Billing Invariant Matrix（资金不变量矩阵）

| Invariant | Scenario | Expected | Observed | Test | Result |
|---|---|---|---|---|---|
| W1 balance≥0 | 全部钱包级用例每步操作后断言 | 余额永不为负 | 全绿，无负余额 | `p0503AssertInvariants`（各用例调用） | PASS |
| W2 frozen≥0 | 全部钱包级用例每步操作后断言 | 冻结永不为负 | 全绿，无负冻结 | `p0503AssertInvariants`（各用例调用） | PASS |
| W3 无超额占用 | 余额 10，8 goroutine 并发 Reserve(10) | 恰好 1 个成功；其余 `ErrInsufficientBalance` | 恰好 1 成功，0 穿透 | `TestP0503_CaseA_ConcurrentReserves_ExactlyOneSucceeds` | PASS |
| W3 部分容量 | 余额 10，3 goroutine 并发 Reserve(6) | 至多 1 个成功（6+6>10） | 恰好 1 成功 | `TestP0503_CaseA_PartialCapacity_StillOneWinner` | PASS |
| W3 网关并发 | 资金仅容 1 请求，2 并发请求同钱包（确定性门控交错） | 恰好 1 个 200，另 1 个 402/500；**恰好 1 次 provider 调用**；账本恰 2 行 | 1×200 + 1×402，executeCalled==1，balance==fund−0.000050 | `TestP0503_Gateway_CaseA_ConcurrentRequests_OneProviderCall` | PASS |
| W4 变动必有账本 | TopUp/Reserve/Commit/Settle/Release 逐一执行后核对行数 | 每次成功变动 +1 证据行（Settle 例外：就地完结，行数不增） | 行数与类型逐一吻合；Settle 后行为 `charge/6.000000/balance_after 19.000000` | `TestP0503_W4_EveryMutationHasLedgerEvidence` | PASS |
| W5 一键一效（仓库级） | 同 key 重放（金额不同）再查询 | 返回原事务（原 tx id、原金额 5），仅 1 行，冻结 5 | 完全一致；金额不匹配校验仅存在于 Transfer（记录在案，不改语义） | `TestP0503_W5_IdempotentReplay_OriginalTransactionOnly` | PASS |
| W5 一键一效（并发） | 8 goroutine 同 idempotency key 并发 Reserve(5) | 全部调用方视角成功且共享同一 tx id；账本恰 2 行；冻结 5 | 全部成功同 tx id，2 行，冻结 5（输家或撞唯一索引报错后被调用方按幂等处理，资金效应恰一次） | `TestP0503_CaseB_ConcurrentDuplicateKeyReserve_OneMoneyEffect` | PASS |
| W5 网关重放 | 同 X-Request-ID 两次请求（第一次成功扣 0.000050） | 重放零钱包动作：余额不变、charge 恰 1 行、总行数 2；重放日志 `WalletCharged=0`、`ErrorCode=""` | 完全一致 | `TestP0503_Gateway_W5_ReplayedRequestID_NoSecondDebit` | PASS |
| W6 provider 只在预留后 | 余额 0 请求 | 402，**0 次 provider 调用**，0 账本行 | 402，executeCalled==0，0 行 | `TestP0503_Gateway_W6_ZeroBalance_NoProviderCall` | PASS |
| W7 settle 仅限 reserved + 不双扣 | 8 goroutine 并发 Settle(6) 同一 tx | 恰好 1 次成功扣款；其余 `ErrTxNotReserved`；余额 44；charge 恰 1 行 | 完全一致 | `TestP0503_CaseD_ConcurrentDuplicateSettle_OneCharge` | PASS |
| W8 release 仅限 valid reserve + 不增余额 | 8 goroutine 并发 Release 同一 tx | 恰好 1 次成功释放；余额不变（50）；其余 `ErrTxNotReserved` | 完全一致 | `TestP0503_CaseE_ConcurrentDuplicateRelease_OneRelease` | PASS |
| W8 上游失败释放 | 预留后 executor 报错 | 502；余额==fund；冻结 0；release 恰 1 行；charge 0 行；日志 Status=failed、WalletCharged=0、ErrorCode 非空 | 完全一致 | `TestP0503_Gateway_AC03_UpstreamFailureAfterReserve_ReleasesHold` | PASS |
| W9 settle/release 竞速 | 预留 8（余额 50）后 16 goroutine 竞速 Settle(8)/Release | 恰好 1 方获胜；败方全部 `ErrTxNotReserved`；终态二选一且账本自洽：{charge, 余额 42, 冻结 0} 或 {release, 余额 50, 冻结 0} | 恰好 1 胜者，终态合法 | `TestP0503_CaseC_SettleReleaseRace_OneLegalFinalState` | PASS |
| W10 超预留结算不多扣 | 预留 20 / 可用 10，Settle(150) | `ErrInsufficientBalance`，钱包分文不动；负数结算同样拒绝 | 余额/冻结/行数全不变 | `TestP0503_CaseG_SettleOverReserve_NeverOverDeducts` | PASS |
| W10 兜底 ≤ 预留 + 证据 | 资金==hold，请求产生巨额成本 0.400020 | 兜底提交恰为 hold（≤预留）；`error_code='undercharged'`；消息含 `shortfall`；`WalletCharged==hold`；`UnderchargeFallbackCounts()["chat|gpt-4o"]` +1；余额恰为 0 | 完全一致 | `TestP0503_Gateway_W10_Undercharge_EvidencePreserved` + `TestP0503_CaseG_SettleOverReserve_NeverOverDeducts`（Commit 恰扣 20.000000） | PASS |
| W11 账本可解释 | 全用例终态：`balance == ledger net`、`frozen == open reserve sum` | 恒等 | 恒等（含 4 并发网关请求终态） | `p0503AssertInvariants` + `TestP0503_Gateway_CaseF_ParallelSuccessfulRequests_ConsistentLedger` + `TestP0503_CaseF_ParallelIndependentOps_LedgerConsistent` | PASS |

**矩阵结论：全部 PASS。未发现任何负余额 / 双扣 / 双释放 / 账本缺失 /
预留前调用 provider / 竞态破坏。**

## 4. Concurrency Results（Cases A–G）

| Case | 场景 | 结果 | 证据 |
|---|---|---|---|
| A | 一钱包两并发预留（含网关级确定性编排） | PASS：恰好一成功、一到 402、恰好一次上游调用 | 钱包级 ×2 + 网关级 ×1 |
| B | 并发重复幂等键预留 | PASS：一个资金效应（同 tx id，2 行账本，冻结 5） | 8 goroutine 同 key |
| C | 并发 Settle+Release | PASS：一个转换获胜，败方全 `ErrTxNotReserved`，终态合法 | 16 goroutine 竞速 |
| D | Settle×N | PASS：一次扣款（余额 44，charge 1 行） | 8 goroutine |
| E | Release×N | PASS：一次释放、余额零变化 | 8 goroutine |
| F | 并行独立请求 | PASS：余额/冻结/账本三方恒等（钱包级混合操作 + 网关级 4 并发成功请求，余额==fund−4×0.000050，4 charge） | 2 套件 |
| G | settle > reserve | PASS：无静默多扣；`ErrInsufficientBalance` 拒绝；兜底提交 ≤ 预留且与 TH-P05-02 钉住语义一致（`undercharged` 证据 + 计数） | 钱包级 + 网关级 |

`-race` 回归：wallet / gateway / idempotency / billing 四包全绿。

## 5. Wallet Historical Anomaly Assessment（历史钱包异常评估）

Batch 2 演练发现的"有余额无账本"钱包复核（只读，未做任何修改）：

| 字段 | 值 |
|---|---|
| 钱包 ID（脱敏） | `3a7a5fef-…-1375e50586ca` |
| 所属用户 | `00000000-0000-0000-0000-000000000000`（引导 admin） |
| balance | 10000.000000 |
| ledger net（重算） | 0.000000（0 行账本） |
| 差异 | +10000.000000 |
| created_at | 2026-09-02 11:36:48.088969+08 |
| updated_at | 2026-09-02 11:36:48.088969+08（与 created 完全相同） |
| version | 0 |

**可能来源**：B2 修复（提交 `8b50201`，2026-09-02 15:29:31）之前的
`ensureAdminUser` 裸 `INSERT INTO wallets … balance '10000'`
（`ENABLE_FAKE_PAYMENT` 时）`ON CONFLICT DO NOTHING`——行特征完全
吻合（version 0、零账本、created==updated、属主为 nil-UUID 引导
admin）。当前 `ProvisionUserWallet` 无法再产生此类行。

**分类：A — TEST_FIXTURE（测试/开发装置数据）。**
证据链：① 行创建时间早于 B2 修复提交 4 小时；② 修复前代码路径形状
完全匹配；③ 属主为引导 admin 而非真实用户；④ 所在数据库仅为本地
开发库（无生产环境存在）；⑤ 本批只读复核确认行未被任何后续变动触碰。

**处置建议**：记录在案即可；仅允许未来的 P5 复核类任务
（TH-P5-RVW-*）按人工复核流程显式处理，**禁止自动改钱包**。

**Blocks TH-P05-09：NO**（分类为 A；且 TH-P05-09 自身仍受
TH-P05-05 / TH-P05-11 TODO 门控）。

## 6. Financial Safety 结论（本任务范围内）

| 项 | 结论 |
|---|---|
| 可能出现负余额 | NO |
| 可能双扣 | NO |
| 可能双释放 | NO |
| 可能在预留前调用 provider | NO |
| 新测试路径存在无账本的钱包变动 | NO |
| 历史钱包-账本不一致 | 仍未解决（分类 A，见 §5，仅经人工复核流程处理） |

## 7. 记录在案的观察（不改语义，供后续任务参考）

- 仓库级 Reserve 幂等重放即使金额不同也返回原事务（金额不匹配校验
  仅存在于 Transfer）——现状钉住，不改。
- 网关预留失败分支对非余额错误返回 500 而非 402——既有行为，
  资金路径仍然安全。
- `internal/pkg/idempotency` 无生产调用点（网关使用裸
  X-Request-ID）——W5 已在网关级覆盖。

## 8. Test Gate

- `go test ./internal/repository/wallet/ ./internal/handler/gateway/ -count=1`：PASS（16/16 新增用例）
- `-race` 回归（wallet / gateway / idempotency / billing）：PASS
- `go vet ./...` / `go build ./...` / gofmt：PASS
- 零生产代码改动（git diff 仅两个 `_test.go` 新文件）

## Result: DONE
